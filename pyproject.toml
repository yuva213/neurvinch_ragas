[project]
name = "neurvinch"
version = "0.1.0"
description = "AI-driven knowledge audit and hybrid RAG reference implementation"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Neurvinch Team" }
]
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
